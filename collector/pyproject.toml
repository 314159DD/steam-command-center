[project]
name = "steam-collector"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "psycopg[binary]>=3.2", "python-dotenv>=1.0"]

[project.optional-dependencies]
dev = ["pytest>=8", "respx>=0.21"]

[tool.pytest.ini_options]
pythonpath = ["."]
